global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'myapp'
    static_configs:
      - targets: ['myapp:8080']
        labels:
          app: 'go-web'

  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
